runtime: go124

main: ./cmd/server

automatic_scaling:
  max_instances: 2

env_variables:
  LC_TEMPLATES_DIR: "./templates"

handlers:
  # Static assets (CSS, JS, images) — served directly by App Engine
  - url: /static
    static_dir: static

  # All other routes → Go app
  - url: /.*
    script: auto
