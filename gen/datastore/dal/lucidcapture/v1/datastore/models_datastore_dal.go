// Code generated by protoc-gen-dal-datastore. DO NOT EDIT.
package dal

import (
	"context"

	dslib "cloud.google.com/go/datastore"
	datastore "github.com/panyam/lucidcapture/gen/datastore/lucidcapture/v1/datastore"
)

// ProjectDatastoreDAL provides database access helper methods for datastore.ProjectDatastore.
type ProjectDatastoreDAL struct {
	// Kind overrides the Datastore kind for all operations.
	// If empty, uses the struct's Kind() method (if any).
	Kind string

	// Namespace overrides the Datastore namespace for all operations.
	// If empty, uses the default namespace.
	Namespace string

	// WillPut hook is called before Put operations.
	// Return an error to prevent the put.
	WillPut func(context.Context, *datastore.ProjectDatastore) error
}

// NewProjectDatastoreDAL creates a new ProjectDatastoreDAL instance.
// If kind is empty, operations will use the struct's Kind() method.
func NewProjectDatastoreDAL(kind string) *ProjectDatastoreDAL {
	return &ProjectDatastoreDAL{Kind: kind}
}

// getKind returns the kind to use for operations.
// Uses the DAL's Kind field if set, otherwise falls back to the struct's Kind() method.
func (d *ProjectDatastoreDAL) getKind() string {
	if d.Kind != "" {
		return d.Kind
	}
	// Fall back to struct's Kind() method
	var entity datastore.ProjectDatastore
	return entity.Kind()
}

// newKey creates a new Datastore key for the given ID.
func (d *ProjectDatastoreDAL) newKey(id string) *dslib.Key {
	key := dslib.NameKey(d.getKind(), id, nil)
	if d.Namespace != "" {
		key.Namespace = d.Namespace
	}
	return key
}

// newIncompleteKey creates a new incomplete Datastore key (for auto-generated IDs).
func (d *ProjectDatastoreDAL) newIncompleteKey() *dslib.Key {
	key := dslib.IncompleteKey(d.getKind(), nil)
	if d.Namespace != "" {
		key.Namespace = d.Namespace
	}
	return key
}

// Put saves a datastore.ProjectDatastore entity to Datastore.
// If the entity's Key field is set, uses that key; otherwise creates a key from the ID field.
// Returns the key used to store the entity.
func (d *ProjectDatastoreDAL) Put(ctx context.Context, client *dslib.Client, obj *datastore.ProjectDatastore) (*dslib.Key, error) {
	// Call WillPut hook if set
	if d.WillPut != nil {
		if err := d.WillPut(ctx, obj); err != nil {
			return nil, err
		}
	}

	// Determine the key to use
	var key *dslib.Key
	if obj.Key != nil {
		key = obj.Key
		// Apply namespace override if set
		if d.Namespace != "" {
			key.Namespace = d.Namespace
		}
	} else if obj.Id != "" {
		key = d.newKey(obj.Id)
	} else {
		key = d.newIncompleteKey()
	}

	// Put the entity
	resultKey, err := client.Put(ctx, key, obj)
	if err != nil {
		return nil, err
	}

	// Update the entity's key
	obj.Key = resultKey

	return resultKey, nil
}

// Get retrieves a datastore.ProjectDatastore entity by key.
// Returns (nil, nil) if the entity is not found.
func (d *ProjectDatastoreDAL) Get(ctx context.Context, client *dslib.Client, key *dslib.Key) (*datastore.ProjectDatastore, error) {
	var entity datastore.ProjectDatastore
	err := client.Get(ctx, key, &entity)
	if err != nil {
		if err == dslib.ErrNoSuchEntity {
			return nil, nil
		}
		return nil, err
	}
	entity.Key = key
	return &entity, nil
}

// Delete removes a datastore.ProjectDatastore entity by key.
func (d *ProjectDatastoreDAL) Delete(ctx context.Context, client *dslib.Client, key *dslib.Key) error {
	return client.Delete(ctx, key)
}

// GetMulti retrieves multiple datastore.ProjectDatastore entities by keys.
// Returns entities in the same order as the keys. Missing entities are nil in the result slice.
func (d *ProjectDatastoreDAL) GetMulti(ctx context.Context, client *dslib.Client, keys []*dslib.Key) ([]*datastore.ProjectDatastore, error) {
	if len(keys) == 0 {
		return []*datastore.ProjectDatastore{}, nil
	}

	entities := make([]datastore.ProjectDatastore, len(keys))
	err := client.GetMulti(ctx, keys, entities)
	if err != nil {
		// Handle partial errors (some entities not found)
		if multiErr, ok := err.(dslib.MultiError); ok {
			result := make([]*datastore.ProjectDatastore, len(keys))
			for i, e := range multiErr {
				if e == nil {
					entities[i].Key = keys[i]
					result[i] = &entities[i]
				} else if e != dslib.ErrNoSuchEntity {
					return nil, err // Return on non-NotFound errors
				}
				// nil for not-found entities
			}
			return result, nil
		}
		return nil, err
	}

	// All entities found
	result := make([]*datastore.ProjectDatastore, len(keys))
	for i := range entities {
		entities[i].Key = keys[i]
		result[i] = &entities[i]
	}
	return result, nil
}

// PutMulti saves multiple datastore.ProjectDatastore entities to Datastore.
// Returns the keys used to store the entities.
func (d *ProjectDatastoreDAL) PutMulti(ctx context.Context, client *dslib.Client, objs []*datastore.ProjectDatastore) ([]*dslib.Key, error) {
	if len(objs) == 0 {
		return []*dslib.Key{}, nil
	}

	// Call WillPut hook for each entity
	if d.WillPut != nil {
		for _, obj := range objs {
			if err := d.WillPut(ctx, obj); err != nil {
				return nil, err
			}
		}
	}

	// Build keys for each entity
	keys := make([]*dslib.Key, len(objs))
	for i, obj := range objs {
		if obj.Key != nil {
			keys[i] = obj.Key
			if d.Namespace != "" {
				keys[i].Namespace = d.Namespace
			}
		} else if obj.Id != "" {
			keys[i] = d.newKey(obj.Id)
		} else {
			keys[i] = d.newIncompleteKey()
		}
	}

	// Put all entities
	resultKeys, err := client.PutMulti(ctx, keys, objs)
	if err != nil {
		return nil, err
	}

	// Update entity keys
	for i, key := range resultKeys {
		objs[i].Key = key
	}

	return resultKeys, nil
}

// DeleteMulti removes multiple datastore.ProjectDatastore entities by keys.
func (d *ProjectDatastoreDAL) DeleteMulti(ctx context.Context, client *dslib.Client, keys []*dslib.Key) error {
	if len(keys) == 0 {
		return nil
	}
	return client.DeleteMulti(ctx, keys)
}

// Query retrieves datastore.ProjectDatastore entities matching the query.
// The caller should create a query using dslib.NewQuery(dal.getKind()).
func (d *ProjectDatastoreDAL) Query(ctx context.Context, client *dslib.Client, q *dslib.Query) ([]*datastore.ProjectDatastore, error) {
	var entities []*datastore.ProjectDatastore
	keys, err := client.GetAll(ctx, q, &entities)
	if err != nil {
		return nil, err
	}

	// Set keys on entities
	for i, key := range keys {
		entities[i].Key = key
	}

	return entities, nil
}

// Count returns the number of entities matching the query.
func (d *ProjectDatastoreDAL) Count(ctx context.Context, client *dslib.Client, q *dslib.Query) (int, error) {
	return client.Count(ctx, q)
}

// GetByID retrieves a datastore.ProjectDatastore entity by ID.
// This is a convenience method that creates a key from the ID.
// Returns (nil, nil) if the entity is not found.
func (d *ProjectDatastoreDAL) GetByID(ctx context.Context, client *dslib.Client, id string) (*datastore.ProjectDatastore, error) {
	key := d.newKey(id)
	return d.Get(ctx, client, key)
}

// DeleteByID removes a datastore.ProjectDatastore entity by ID.
// This is a convenience method that creates a key from the ID.
func (d *ProjectDatastoreDAL) DeleteByID(ctx context.Context, client *dslib.Client, id string) error {
	key := d.newKey(id)
	return d.Delete(ctx, client, key)
}

// GetMultiByIDs retrieves multiple datastore.ProjectDatastore entities by IDs.
// This is a convenience method that creates keys from the IDs.
// Returns entities in the same order as the IDs. Missing entities are nil in the result slice.
func (d *ProjectDatastoreDAL) GetMultiByIDs(ctx context.Context, client *dslib.Client, ids []string) ([]*datastore.ProjectDatastore, error) {
	if len(ids) == 0 {
		return []*datastore.ProjectDatastore{}, nil
	}

	keys := make([]*dslib.Key, len(ids))
	for i, id := range ids {
		keys[i] = d.newKey(id)
	}

	return d.GetMulti(ctx, client, keys)
}

// StepDatastoreDAL provides database access helper methods for datastore.StepDatastore.
type StepDatastoreDAL struct {
	// Kind overrides the Datastore kind for all operations.
	// If empty, uses the struct's Kind() method (if any).
	Kind string

	// Namespace overrides the Datastore namespace for all operations.
	// If empty, uses the default namespace.
	Namespace string

	// WillPut hook is called before Put operations.
	// Return an error to prevent the put.
	WillPut func(context.Context, *datastore.StepDatastore) error
}

// NewStepDatastoreDAL creates a new StepDatastoreDAL instance.
// If kind is empty, operations will use the struct's Kind() method.
func NewStepDatastoreDAL(kind string) *StepDatastoreDAL {
	return &StepDatastoreDAL{Kind: kind}
}

// getKind returns the kind to use for operations.
// Uses the DAL's Kind field if set, otherwise falls back to the struct's Kind() method.
func (d *StepDatastoreDAL) getKind() string {
	if d.Kind != "" {
		return d.Kind
	}
	// Fall back to struct's Kind() method
	var entity datastore.StepDatastore
	return entity.Kind()
}

// newKey creates a new Datastore key for the given ID.
func (d *StepDatastoreDAL) newKey(id string) *dslib.Key {
	key := dslib.NameKey(d.getKind(), id, nil)
	if d.Namespace != "" {
		key.Namespace = d.Namespace
	}
	return key
}

// newIncompleteKey creates a new incomplete Datastore key (for auto-generated IDs).
func (d *StepDatastoreDAL) newIncompleteKey() *dslib.Key {
	key := dslib.IncompleteKey(d.getKind(), nil)
	if d.Namespace != "" {
		key.Namespace = d.Namespace
	}
	return key
}

// Put saves a datastore.StepDatastore entity to Datastore.
// If the entity's Key field is set, uses that key; otherwise creates a key from the ID field.
// Returns the key used to store the entity.
func (d *StepDatastoreDAL) Put(ctx context.Context, client *dslib.Client, obj *datastore.StepDatastore) (*dslib.Key, error) {
	// Call WillPut hook if set
	if d.WillPut != nil {
		if err := d.WillPut(ctx, obj); err != nil {
			return nil, err
		}
	}

	// Determine the key to use
	var key *dslib.Key
	if obj.Key != nil {
		key = obj.Key
		// Apply namespace override if set
		if d.Namespace != "" {
			key.Namespace = d.Namespace
		}
	} else if obj.Id != "" {
		key = d.newKey(obj.Id)
	} else {
		key = d.newIncompleteKey()
	}

	// Put the entity
	resultKey, err := client.Put(ctx, key, obj)
	if err != nil {
		return nil, err
	}

	// Update the entity's key
	obj.Key = resultKey

	return resultKey, nil
}

// Get retrieves a datastore.StepDatastore entity by key.
// Returns (nil, nil) if the entity is not found.
func (d *StepDatastoreDAL) Get(ctx context.Context, client *dslib.Client, key *dslib.Key) (*datastore.StepDatastore, error) {
	var entity datastore.StepDatastore
	err := client.Get(ctx, key, &entity)
	if err != nil {
		if err == dslib.ErrNoSuchEntity {
			return nil, nil
		}
		return nil, err
	}
	entity.Key = key
	return &entity, nil
}

// Delete removes a datastore.StepDatastore entity by key.
func (d *StepDatastoreDAL) Delete(ctx context.Context, client *dslib.Client, key *dslib.Key) error {
	return client.Delete(ctx, key)
}

// GetMulti retrieves multiple datastore.StepDatastore entities by keys.
// Returns entities in the same order as the keys. Missing entities are nil in the result slice.
func (d *StepDatastoreDAL) GetMulti(ctx context.Context, client *dslib.Client, keys []*dslib.Key) ([]*datastore.StepDatastore, error) {
	if len(keys) == 0 {
		return []*datastore.StepDatastore{}, nil
	}

	entities := make([]datastore.StepDatastore, len(keys))
	err := client.GetMulti(ctx, keys, entities)
	if err != nil {
		// Handle partial errors (some entities not found)
		if multiErr, ok := err.(dslib.MultiError); ok {
			result := make([]*datastore.StepDatastore, len(keys))
			for i, e := range multiErr {
				if e == nil {
					entities[i].Key = keys[i]
					result[i] = &entities[i]
				} else if e != dslib.ErrNoSuchEntity {
					return nil, err // Return on non-NotFound errors
				}
				// nil for not-found entities
			}
			return result, nil
		}
		return nil, err
	}

	// All entities found
	result := make([]*datastore.StepDatastore, len(keys))
	for i := range entities {
		entities[i].Key = keys[i]
		result[i] = &entities[i]
	}
	return result, nil
}

// PutMulti saves multiple datastore.StepDatastore entities to Datastore.
// Returns the keys used to store the entities.
func (d *StepDatastoreDAL) PutMulti(ctx context.Context, client *dslib.Client, objs []*datastore.StepDatastore) ([]*dslib.Key, error) {
	if len(objs) == 0 {
		return []*dslib.Key{}, nil
	}

	// Call WillPut hook for each entity
	if d.WillPut != nil {
		for _, obj := range objs {
			if err := d.WillPut(ctx, obj); err != nil {
				return nil, err
			}
		}
	}

	// Build keys for each entity
	keys := make([]*dslib.Key, len(objs))
	for i, obj := range objs {
		if obj.Key != nil {
			keys[i] = obj.Key
			if d.Namespace != "" {
				keys[i].Namespace = d.Namespace
			}
		} else if obj.Id != "" {
			keys[i] = d.newKey(obj.Id)
		} else {
			keys[i] = d.newIncompleteKey()
		}
	}

	// Put all entities
	resultKeys, err := client.PutMulti(ctx, keys, objs)
	if err != nil {
		return nil, err
	}

	// Update entity keys
	for i, key := range resultKeys {
		objs[i].Key = key
	}

	return resultKeys, nil
}

// DeleteMulti removes multiple datastore.StepDatastore entities by keys.
func (d *StepDatastoreDAL) DeleteMulti(ctx context.Context, client *dslib.Client, keys []*dslib.Key) error {
	if len(keys) == 0 {
		return nil
	}
	return client.DeleteMulti(ctx, keys)
}

// Query retrieves datastore.StepDatastore entities matching the query.
// The caller should create a query using dslib.NewQuery(dal.getKind()).
func (d *StepDatastoreDAL) Query(ctx context.Context, client *dslib.Client, q *dslib.Query) ([]*datastore.StepDatastore, error) {
	var entities []*datastore.StepDatastore
	keys, err := client.GetAll(ctx, q, &entities)
	if err != nil {
		return nil, err
	}

	// Set keys on entities
	for i, key := range keys {
		entities[i].Key = key
	}

	return entities, nil
}

// Count returns the number of entities matching the query.
func (d *StepDatastoreDAL) Count(ctx context.Context, client *dslib.Client, q *dslib.Query) (int, error) {
	return client.Count(ctx, q)
}

// GetByID retrieves a datastore.StepDatastore entity by ID.
// This is a convenience method that creates a key from the ID.
// Returns (nil, nil) if the entity is not found.
func (d *StepDatastoreDAL) GetByID(ctx context.Context, client *dslib.Client, id string) (*datastore.StepDatastore, error) {
	key := d.newKey(id)
	return d.Get(ctx, client, key)
}

// DeleteByID removes a datastore.StepDatastore entity by ID.
// This is a convenience method that creates a key from the ID.
func (d *StepDatastoreDAL) DeleteByID(ctx context.Context, client *dslib.Client, id string) error {
	key := d.newKey(id)
	return d.Delete(ctx, client, key)
}

// GetMultiByIDs retrieves multiple datastore.StepDatastore entities by IDs.
// This is a convenience method that creates keys from the IDs.
// Returns entities in the same order as the IDs. Missing entities are nil in the result slice.
func (d *StepDatastoreDAL) GetMultiByIDs(ctx context.Context, client *dslib.Client, ids []string) ([]*datastore.StepDatastore, error) {
	if len(ids) == 0 {
		return []*datastore.StepDatastore{}, nil
	}

	keys := make([]*dslib.Key, len(ids))
	for i, id := range ids {
		keys[i] = d.newKey(id)
	}

	return d.GetMulti(ctx, client, keys)
}
